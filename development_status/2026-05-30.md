# Development Status — 30 May 2026

## Done

### Project Setup
- Folder structure created (backend + frontend separated)
- README, TODO, PROJECT_RULES, CURRENT_STATUS written
- `.gitignore` configured
- Git repo initialised, 6 commits on `main`

### Backend
- FastAPI app with CORS configured (`main.py`)
- `config.py` centralises all constants (paths, thresholds, extensions, CORS)
- `POST /api/upload` fully working
  - Accepts multiple files
  - Validates file extensions (`.py`, `.java`, `.cs`, `.js`)
  - Validates file size (max 10 MB)
  - Creates unique session folder under `backend/uploads/<session_id>/`
  - Returns `session_id`, `uploaded_files`, `file_count`
- Pydantic schemas defined (`UploadResponse`, `SimilarityPair`, `AnalysisResponse`)
- CodeBERT embedding generation implemented (`embedding_service.py`)
  - Model: `microsoft/codebert-base`
  - Lazy loading + module-level cache
  - Returns 768-dimensional `[CLS]` vector per file
  - Model downloaded and cached locally

### Tests
- Sample files created (`original.py`, `renamed_variables.py`, `unrelated.py`)
- `tests/test_embeddings.py` runs and confirmed working
- Upload endpoint tested via Swagger — `200 OK`
- File type validation tested — `400` on `.txt` upload

---

## Working Right Now

| Feature | Status |
|---|---|
| `GET /` health check | Working |
| `POST /api/upload` | Working |
| Session folder creation | Working |
| File extension validation | Working |
| CodeBERT embeddings | Working |
| Swagger UI at `/docs` | Working |

---

## TODO

### Day 3 — Similarity (next)
- [ ] Implement `similarity_service.py` (cosine similarity with scikit-learn)
- [ ] Wire `embedding_service` + `similarity_service` together
- [ ] Implement `POST /api/analyze` endpoint
- [ ] Re-enable `similarity_routes` in `main.py`
- [ ] Return ranked suspicious pairs with `high / medium / low` level
- [ ] Write `tests/test_similarity.py`

### Day 4 — Frontend
- [ ] Upload page (file picker + submit button)
- [ ] Call `POST /api/upload` and `POST /api/analyze` from frontend
- [ ] Results page with similarity table
- [ ] Side-by-side file comparison view

### Day 5 — Polish + Demo
- [ ] Bug fixing
- [ ] UI improvements
- [ ] Prepare demo with sample files
- [ ] Optional: Ollama explanation integration

---

## Not Doing
- Docker
- Database
- Authentication
- FAISS
- PDF export
- Tauri desktop app
